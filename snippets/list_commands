# List all the available commands
list_commands() {
    output=
    for command in ${commands}
    do
        if [[ ${output} ]]; then
            output="${output}${command}"
        else
            output="${command}\n"
        fi
    done
    echo -e "${output}"
}
