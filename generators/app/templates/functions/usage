usage()
{
cat << EOF
usage: $0 [-v<% for(var i=0; i<flags.length; i++) {-%><%= flags[i].flagShort -%><% } -%>] <% for(var i=0; i<options.length; i++) {-%>[-<%= options[i].varShort -%> VALUE] <% } -%>

<% if (description) {-%>

<%= description -%>

<% } -%>

<% if (options.length>0) {-%>Options:
<% for(var i=0; i<options.length; i++) {-%>
<% if (options[i].varShort.length) {-%>-<%= options[i].varShort -%><% } -%><% if (options[i].varShort.length && options[i].varLong.length) {-%>, --<%= options[i].varLong -%><% } -%>
<% if (!options[i].varShort.length && options[i].varLong.length) {-%>--<%= options[i].varLong -%><% } -%>
   <%= options[i].varDesc -%>

<% } -%>
<% } -%>

<% if (flags.length>0) {-%>Flags:
<% for(var i=0; i<flags.length; i++) {-%>
<% if (flags[i].flagShort.length) {-%>-<%= flags[i].flagShort -%><% } -%><% if (flags[i].flagShort.length && flags[i].flagLong.length) {-%>, --<%= flags[i].flagLong -%><% } -%>
<% if (!flags[i].flagShort.length && flags[i].flagLong.length) {-%>--<%= flags[i].flagLong -%><% } -%>
   <%= flags[i].flagDesc -%>

<% } -%>
<% } -%>

Standard Options:
  -v         Verbose mode
  --help     Show this help
  --version  Show script version
EOF
}
