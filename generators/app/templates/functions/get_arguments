get_arguments()
{
    ARGS="<% for(var i=0; i<args.length; i++) {-%><%= args[i].varName%><% if ((i+1)<args.length) {-%> <% } -%><% } -%>"

    shift $(( ARGSHIFT - 1 ))

    for ARG in $ARGS
    do
      if [ ! -z "$1" ]; then
        eval "$ARG=$1"
      fi
      shift
    done
}
